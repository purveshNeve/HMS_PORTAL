import { ManagerDashboard } from "@/components/managerDashboard/ManagerDashboard";

export default function DashboardPage() {
  return (
    <div>
      {/* your existing page title / layout stays untouched */}
      <ManagerDashboard />
    </div>
  );
}